"use client";

import { DutyCalendar, type DutyType } from "@/components/duty-calendar";

// Календарь дежурств трейдеров. Смены заданы кодом, а не правятся на странице:
// в перенесённых из Supabase данных они уже расставлены этими названиями, и
// переименование задним числом рассыпало бы раскраску истории.
//
// Порядок списка задаёт порядок внутри дня: смены идут по расписанию, не по
// алфавиту. «Отпуск» встречается в перенесённых данных, поэтому в списке есть,
// но своего цвета не имеет — рисуется нейтрально.
const TRADER_SHIFTS: readonly DutyType[] = [
  { name: "Утро", color: "#93C5FD" },
  { name: "Вечер", color: "#8B5A2B" },
  { name: "Отгул", color: "#EF4444" },
  { name: "ДСВД", color: "#FACC15" },
  // Без цвета — так эта страница выглядела с самого начала; менять вид рабочего
  // календаря заодно с переносом на общий компонент было бы подлогом.
  { name: "Отпуск" },
];

export default function TempCalendarPage() {
  return (
    <DutyCalendar
      apiPath="/api/temp-calendar"
      storageKey="temp-calendar-admin-password"
      subtitle="Временный календарь дежурств (Supabase временно недоступен)"
      footer="Это временная страница на отдельном хранилище (Vercel Blob), не связанная с основной базой данных. После восстановления лимитов Supabase дежурства нужно будет перенести в основной календарь вручную."
      fixedTypes={TRADER_SHIFTS}
    />
  );
}
